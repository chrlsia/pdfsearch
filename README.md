# ⚠️ Important (your project!)

This tool depends on:

👉 pdftotext

So users must still install:

## linux

sudo apt install poppler-utils

## macOS

brew install poppler

## Windows

Install Poppler manually

Add to PATH

